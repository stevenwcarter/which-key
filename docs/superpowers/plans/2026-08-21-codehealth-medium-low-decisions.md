# code-health B14–B43 — decisions record

Companion to `2026-08-21-codehealth-medium-low.md` (the plan) and
`../specs/2026-08-21-codehealth-medium-low-design.md` (the spec).

This file exists because the execution ledger lived in a git-ignored scratch
directory. These are the calls made on the maintainer's behalf during execution,
and the observations parked rather than acted on — preserved so they outlive
the workspace.

Branch: `codehealth/2026-08-21-batch2`. 28 findings fixed across 47 commits;
244 → 313 tests; coverage 98.83% → 99.65% statements, 94.58% → 96.79% branches.
Five findings the batch itself discovered (B44–B47) and two the maintainer left
unchecked (B15, B41) remain open in `bughunt.md`, alongside 7 decision-needed markers.

## Rulings (23)

Each is a decision taken without asking, with what it costs if wrong.

- Ruling 1 (Task 6, B20): T6's rewrite calls `getActiveGroup(subPrefix)` for EVERY candidate, including a pure leaf with no deeper sequence — so a registered group label now wins over the leaf's own description even in the non-colliding case. This is a behaviour change beyond the finding's literal text. ACCEPTED: it is the same precedence Task 13 (B37) establishes for the cheatsheet — a label the consumer explicitly registered should surface — and keeping the two paths consistent is worth more than a literal reading. Cost if wrong: a consumer who registered both a group label and a leaf description at the same key sees the group label in the popup where they previously saw the leaf's; recoverable by reverting one ternary.

- Ruling 2 (Tasks 15, 22, 25, 26 — docs/API.md TOC): four tasks edit the same table of contents. T25 Step 6 and T26 Step 4 both rewrite TOC entries and could silently revert T15's `useWhichKeyState(what?)` and T22's `=> WhichKeyMountHandle` heading changes. DECIDED: carry an explicit instruction in the T25 and T26 dispatches to ADD entries and renumber only, never to rewrite existing entry text, and to re-read the TOC immediately before editing. Cost if wrong: a stale TOC entry pointing at a renamed heading — cosmetic, caught by the final review.

- | 18→19 | styles.css, styles-contract.test.ts | T19 extends T18's file | See Ruling 3 (import nit) |

- Ruling 4 (Task 15): `useWhichKeyState` gains an optional `what?: string` param — a public signature change on a documented export. ACCEPTED during plan self-review over the alternative (a separate internal warn hook), which would emit two warnings for one mistake. The param is documented in T15 Step 8 per CLAUDE.md's named-exports rule. Cost if wrong: a slightly odd public signature; removable later without breaking callers since it is optional.

- Ruling 5 (bughunt.md checkbox state): `bughunt.md` is currently modified-but-uncommitted with the user's `[x] execute` selections. Left as-is, Task 1's diff would carry 28 unrelated checkbox changes and the reviewer would flag them. DECIDED: commit the selections as their own commit before Task 1 so every task diff shows only its own finding being stripped. Cost if wrong: one extra commit in the log.

- Ruling 6 (scope): B15 and B41 were left unchecked by the user and stay open; the 7 decision-needed markers are never auto-applied. No `[x] skip` items exist, so nothing is appended to feedback_codehealth_skip.md. Cost if wrong: none — this is the user's recorded selection.

- Ruling 7 (Task 1, B27): eslint-plugin-react-hooks@7.1.1's `configs.recommended` ships 16 rules, not the 2 (`rules-of-hooks`, `exhaustive-deps`) that finding B27's evidence named — it adds React-Compiler readiness rules (refs, purity, immutability, set-state-in-effect, ...). One of them, `react-hooks/refs`, flagged the ref-read-during-render idiom in WhichKeyProvider and forced a production change. ACCEPTED: B27's proposed fix says "append their recommended flat configs" verbatim; the rules are dev-only with zero runtime or bundle impact; and the finding they surfaced was genuine (a real React-Compiler incompatibility), fixed behaviour-preservingly rather than suppressed. An existing unmodified StrictMode test (src/react/__tests__/WhichKeyLayer.test.tsx:75-95) empirically pins the engine-constructed-once invariant the refactor depends on. Cost if wrong: future contributors are gated by 14 rules the project never deliberately chose; narrowing to the two named rules is a two-line config edit.

- Task 3: Ruling 8 — CORRECTION to my own pre-dispatch direction. I told the implementer a combined guard was safe because "a subsequent prefix-only keystroke would hide the stale popup anyway." The reviewer independently disproved that: a chain of leaf-AND-prefix keystrokes (register g, g h, g h x — each both leaf and prefix) never reaches the prefix-only branch's hide, so the stale popup survives. The window IS bounded, but by (chain depth x timeoutMs) via eventual resetBuffer — incidental, not designed. My mechanism was wrong; the conclusion (not a blocker for B21) still holds: no disclosure (the stale content was already on screen pre-taint), no regression (baseline had no refresh at all), and the real fix mirrors the prefix-only hide, which exceeds B21's scope. DECIDED: recorded the gap as a new finding B44 in bughunt.md (commit 9d925e9) rather than silently expanding Task 3 or leaving it only in a report that gets deleted with the workspace. Cost if wrong: a stale popup can linger up to chain-depth x timeoutMs in an app whose keymap makes every prefix level independently fireable — cosmetic, no leak, and B44 carries the fix.

- Tasks 4+5: Ruling 9 — the reviewer found that Task 4's hoist changed exactly the one Escape branch with ZERO test coverage: no test anywhere registers a compound sequence ending in Escape (e.g. 'g Escape') and presses it with a non-empty buffer. matcher.test.ts:199 covers the cancel path (no leaf) and :216 covers a bare 'Escape' leaf with an EMPTY buffer (guard skipped entirely). So my plan's justification — "no new test by design (the existing Escape tests are the guard)" — is false for the branch that actually moved. UPHELD AGAINST MY OWN PLAN: the user's CLAUDE.md names this exact pattern ("no test needed because of invariant X") a red flag and says the declined test is usually the load-bearing one. A user instruction outranks my plan. Entering fix round 1 to add the characterization test. Cost if wrong: one extra test; near zero.

- Task 6: Ruling 10 — the implementer deviated from my plan's literal Step-3 code and was RIGHT to. My version resolved the description as `getActiveGroup(subPrefix) ?? existing?.description ?? top.description` unconditionally. I hand-traced it and confirmed their claim: in reverse registration order (['g h i','g h']) the first iteration processes the DEEPER entry, where `top` is the entry at the longer key 'g h i', so `top.description` ('Deeper') gets planted as a false stand-in for the group label; the second iteration then picks it up via `existing?.description` and the result is 'Deeper' instead of the leaf's 'Leaf label'. My own plan's first test would have failed. Their correction scopes `top.description` to the `!isGroup` case only — i.e. it is used only when THIS iteration's entry is the exact leaf at subPrefix. UPHELD: the deviation fixes a real order-dependence my code reintroduced, which is the very defect B20 exists to remove. The pre-ruled behaviour (getActiveGroup consulted for every candidate, Ruling 1) is preserved unchanged. Cost if wrong: description precedence on a merged row differs from intent; three tests pin it in both orders.

- Task 8: Ruling 11 — warning-copy tense split. The two new strings end "...; shortcut not registered." while the sibling they were modelled on (src/react/useShortcut.ts:19) ends "...; shortcut will not register." The reviewer is right that this would be permanent and visible once Task 28 publishes the whole warning family in one table. NOT entering a fix loop, because the premise is transient: Task 15 (B24) replaces useShortcut.ts:19, useShortcutGroup.ts:12 and WhichKeyLayer.tsx:15 wholesale with the unified warnNoProvider message ("[whichkey] <what> used outside <WhichKeyProvider>; wrap your app in <WhichKeyProvider> for it to work."), which drops "will not register" entirely and is a differently-shaped message anyway (a provider-setup instruction, not a rejection notice). So "not registered" stands as canonical for the rejection family and no inconsistency reaches Task 28. Enforcement: Task 15's dispatch will require removing those three strings, and Task 28's dispatch will require verifying the whole family reads consistently before publishing the table. Cost if wrong: if Task 15 were to leave a string behind, Task 28 documents two tenses — caught by that added check.

- Task 8: Ruling 12 — the reviewer's Minor about six tasks duplicating `.replace(/^whichkey:\s*/, '')` overstates the count: only Tasks 9 (registerGroup) and 10 (helpKey) compose an inner Error message; Tasks 11, 12, 20 and 21 warn about values, not caught Errors, so they need no strip. That is 3 sites total. DECIDED: Task 9 is the second user, which is the right moment to extract — its dispatch will require pulling the strip into a small shared internal helper and refactoring Task 8's site onto it, so Task 10 has one to use. Cost if wrong: a one-line helper nobody else needs; trivially inlined back.

- Task 10: Ruling 13 — bughunt.md's B23 evidence contained two inaccuracies: it cited helpKey="ctrl/" as throwing (it does not — no '+' means parseKey treats it as one literal base key), and it listed "an empty string" as a throw case (helpKey:'' is falsy, so the `if (helpKey)` guard skips registration entirely and nothing ever reaches parseKey). Neither undermines the finding — 'Hyper+/', a trailing '+', and other unknown modifiers genuinely throw, and the React render-time teardown is real and now fixed. No action beyond recording it; B23 is stripped. Cost if wrong: none, the fix covers every genuinely-throwing input.

- Task 12: Ruling 14 — the reviewer flagged that `engine.activateLayer(level, exclusive)` is a public method with "the identical unreachable-layer failure mode". I probed all three paths against the built bundle rather than accepting that: `pushLayer({level:-1})` now warns and stays reachable (B34 fixed); `activateLayer(-1,true)` is INERT — nextLevel() and blockLevel() both ignore a negative-level layer and nothing registers at that level, so no shortcut becomes unreachable; but `register('z', fn, { level: -1 })` IS silently unreachable with no warning. So the reviewer identified the right class of gap and the wrong method. Recorded the real one as finding B45 (commit 5a28029) rather than acting on it here — it is a different public entry point from B34's, and Task 25 is about to document `level` as a public option, which makes shipping it unvalidated more visible. Cost if wrong: a consumer hand-setting a negative level still gets a silent dead shortcut; B45 carries the fix.

- Task 14: Ruling 15 — the implementer deviated from my brief by adding the /i flag, and was RIGHT to. My brief asserted "'macOS' already matches the existing /Mac|.../ regex, so the regex itself needs no change." That is false: userAgentData.platform reports 'macOS' with a lowercase m, and the pattern is case-sensitive, so the new userAgentData branch would have silently failed to detect a Mac — i.e. my "fix" would have introduced a regression on exactly the modern API it was adopting. I verified independently: /Mac|iPod|iPhone|iPad/.test('macOS') === false, with the i flag true. I also swept the i flag against 16 realistic platform strings (MacIntel, Win32, Linux x86_64, Linux armv8l, Android, Chrome OS, iOS, FreeBSD amd64, SunOS, ...) and it adds exactly one new match — 'macOS' — with no false positives. UPHELD. Cost if wrong: a platform string containing 'mac'/'ipad' in some unrelated sense would be misread as Apple; none exists in the standard UA-CH or navigator.platform value sets.

- Task 17: Ruling 16 — PLAN DEFECT of mine, found by Task 17. There is a pre-existing `src/__tests__/class-contract.test.tsx` (from the earlier B1-B13 batch) that asserts the vanilla and React renderers emit EXACTLY the documented class set — a strict set equality, not a subset — and whose own header comment states the process: "Adding a class to a renderer means adding it here AND to both doc tables." My pre-flight scan never found this file, so (a) Task 17's new `wk-popup-host` class breaks it, and (b) Task 19 was planned to CREATE a mechanical emitted-vs-defined test that substantially duplicates it. Correcting the implementer's framing: this is NOT a "pre-existing, out-of-scope failure". The suite was 294/294 green at Task 16; the test broke because Task 17 emitted a new class. It is Task 17's to resolve. DECIDED (a): follow the test file's own documented process — add `wk-popup-host` to its CONTRACT array and to both doc tables (README Styling + docs/API.md CSS class contract). Editing that array is the sanctioned maintenance path the file itself prescribes, not a test refactor to accommodate a bug. The host is a real element in the consumer's DOM; hiding it from the documented contract would be dishonest, and a consumer may legitimately need to restyle the wrapper's stacking context. Rejected the alternative of giving the host a `data-` attribute instead of a class — that would dodge the contract rather than extend it, and would leave the wrapper unstylable. DECIDED (b): revise Tasks 18/19. Task 18 still creates `styles-contract.test.ts` but ONLY for the z-index custom-property assertions, which are about stylesheet VALUES and genuinely new. Task 19's "every emitted class has a CSS rule" check goes into the EXISTING class-contract.test.tsx — that file already collects emitted classes and merely doesn't assert they are styled — with `wk-popup-host` exempt as a deliberately unstyled structural wrapper. Cost if wrong: `wk-popup-host` becomes a documented class the project must keep emitting; it is a thin wrapper, so the commitment is small and reversible before 1.0.

- Task 18: Ruling 17 — the implementer deviated from my brief's "no hardcoded z-index" regex and was RIGHT to. My pattern was `/z-index:\s*(?!var\()[^;]+;/g`; because `\s*` sits OUTSIDE the negative lookahead, the engine backtracks it to zero width and evaluates `(?!var\()` against the space after the colon, which trivially succeeds — so the assertion matched `z-index: var(--wk-z-index, 1000);` and would have FAILED against a correct fix. Verified in node: my regex returns a match on the correct line, theirs returns null; both still catch `z-index: 50;`. Their correction moves the whitespace inside the lookahead: `/z-index:(?!\s*var\()\s*[^;]+;/g`. UPHELD — my version would have red-flagged the very implementation it was written to verify, pressuring a pointless fix loop or a weakened assertion. Cost if wrong: none identified; the corrected pattern is strictly better on both the positive and negative case.

- Task 19: Ruling 18 — I asked for ONE named exemption set routing both cases; the implementer shipped TWO (VANILLA_ONLY, UNSTYLED) that today hold the same single element. ACCEPTED. They model genuinely different concerns — "emitted only by the vanilla renderer" versus "deliberately unstyled" — and a future class could satisfy one without the other, so collapsing them would overload one name and invite a wrong exemption later. I explicitly invited judgment on this item and the reasoning is better than my instruction was. Cost if wrong: a future class needing both exemptions must be listed twice; the sets are adjacent and commented, so the drift risk is small.

- Task 22: Ruling 19 — the implementer added an explicit `<a id="mountwhichkey">` anchor instead of relying on GitHub's auto-slug, deviating from my brief. UPHELD. I verified the pre-existing TOC link `#mountwhichkeyengine-options` did not match the old heading `mountWhichKey(engine, options?) => { unmount() }`, so that link was already broken; an explicit anchor makes the TOC->heading pair resolve deterministically instead of depending on slug rules for a heading containing `=> { unmount() }`, and it follows the file's own `engine.register` precedent. Disclosed prominently rather than slipped in. Cost if wrong: one TOC row links via a custom id rather than its visible text — routed to Task 25 below.

- Tasks 23+24: Ruling 20 — the implementer also fixed a SECOND dangling relative link (./CONTRIBUTING.md) beyond the one B43's headline names. UPHELD as in scope, not creep: my brief's own Step 3 told them to grep for all such links and "fix each the same way", with the stay-relative exception limited to targets inside `files`, and CONTRIBUTING.md is not. B43's evidence text generalises too. Opening a separate finding for a second instance of the identical bug class, surfaced by the same brief's own step, would have been bureaucratic. Cost if wrong: one extra link converted to absolute in the README.

- Task 26: Ruling 21 — the implementer found 2 of the 4 code samples in my brief were factually wrong and corrected them. VERIFIED against the built bundle myself: parseKey('Shift+/') returns '/', NOT '?' (and emits a warning telling the consumer to write '?' directly); parseKey('n') returns 'n', NOT 'N' (uppercasing applies under a modifier or to a bare uppercase letter — parseKey('Shift+n') and parseKey('N') both give 'N'). UPHELD. Publishing a wrong example in a section whose entire purpose is helping people debug canonicalization would have actively misled the exact reader it targets.

- Task 28: Ruling 22 — the implementer reworded the classPrefix warning in SOURCE during a docs-only task. UPHELD, and it was sanctioned in the dispatch: this was the last point before publishing the warning family as a reference table, and the old text ("not starting with a digit") was imprecise about the regex's other edge. No test broke because the existing assertion used stringContaining on a prefix, which the reviewer verified directly. Cost if wrong: a denser warning string; accurate to /^-?[A-Za-z_][A-Za-z0-9_-]*$/.

- Task 28: Ruling 23 — the brief's draft README prose repeated the same 'Shift+/' -> '?' error I had already hit in Task 26 (I had copied it from bughunt.md, which inherited it from CLAUDE.md). Caught again and corrected; published README and docs/API.md now agree with each other and with reality.

## Deferred observations (24)

Raised in review, judged not worth acting on during the batch. Recorded so a
future pass can re-judge rather than rediscover them.

- Task 1: minor (deferred): report justified skipping a test for the WhichKeyProvider refactor with a behaviour-identical prose argument (the "no test needed because of invariant X" pattern CLAUDE.md flags). Mitigated here by an existing passing StrictMode test; hold later tasks on this file to the same bar.
- Task 1: minor (deferred): the broader react-hooks preset may trip rules Tasks 15/16 briefs did not anticipate (purity, set-state-in-render). Carry as a note into those dispatches.
- Task 2: minor (deferred): new test ids ('deeper') diverge from the sibling block's 'seq' naming convention in matcher.test.ts. Cosmetic.
- Task 2: minor (deferred): report did not show per-commit stat, so "the RED commit is test-only" was not independently checkable from the report alone.
- Tasks 4+5: minor (deferred): Task 5 also ships untested, but `this.timer !== null` has no consumer outside clearTimer()'s own null-guard, so there is no user-visible behaviour to pin today.
- Task 6: minor (deferred): test 3 (no-collision leaf case) passes pre- and post-fix by design — a characterization pin, not a bug-catcher; honestly reported as such.
- Task 6: minor (deferred): the ~18-line comment block could shed one parenthetical clause.
- Task 6: minor (deferred): getActiveGroup is now called once per colliding entry rather than once per group row — pure read, consistent with Ruling 1, theoretical only.
- Task 8: minor (deferred): the invalid-key-string warning repeats the key string twice (outer wrap plus keys.ts's own clause) — cosmetic, arguably useful.
- Task 9: minor (deferred): docs/API.md:82's registerGroup example was not revisited; Task 25's doc cross-check should catch it.
- Task 10: minor (deferred): helpKey:'' silently disables help with no warning, same as helpKey:null, even though '' is a non-null "unparseable" value. Pre-existing, outside B23's scope, unchanged by this diff.
- Task 11: minor (deferred): the validating IIFE is slightly unconventional versus a named helper; specified verbatim in the brief and well-commented.
- Task 12: minor (deferred): the undercut heuristic uses `nextLevel() - 1` as a proxy for the private blockLevel(), which counts only exclusive layers — so it can over-warn when the highest active layer is non-exclusive. One-directional (never fails to warn on a genuinely blocked level) and hedged with "may". Plan-mandated.
- Task 14: minor (deferred): a narrower fix (explicit 'macOS' alternative, or case-folding only at the comparison) would be marginally tighter than a blanket /i; shipped version verified safe.
- Task 15: minor (deferred): the WhichKeyLayer no-provider test's assertions ('<WhichKeyLayer>' and 'outside <WhichKeyProvider>') were both satisfiable by the OLD message too, so its RED came from the missing helper import rather than a label mismatch. The branch is genuinely exercised against current code and would catch a dropped call, so this is a rigour note, not a gap — the old message no longer exists to be confused with.
- Task 17: minor (deferred, ROUTED TO TASK 19): the class tables' surrounding prose says "Override any class to customize appearance", which oversells now that one row is a deliberately unstyled structural wrapper. Task 19 already edits both tables — fold a one-line note in there.
- Task 17: minor (deferred, ROUTED TO TASK 19): CONTRACT is now asymmetric between the two renderer assertions (React compares against a filtered subset via a single hardcoded exclusion). Task 19 needs a real exemption-set mechanism rather than another hardcoded filter.
- Task 18: minor (deferred): the corrected regex is case-sensitive and does not exclude CSS comments, so `Z-INDEX: 50;` or a commented-out declaration would slip past. Inherited from my brief, not a regression; the sheet contains neither.
- Task 19: minor (deferred): the implementer reported "Concerns: none" while having made and reasoned through the two-set trade-off elsewhere in the same report — the short contract undersold a real judgment call.
- Task 20: minor (deferred, NEW FRAGILITY introduced by this task): mount.test.ts never wraps ui.unmount() in try/finally, so an assertion failure earlier in the file now cascades a spurious duplicate-mount warning and a no-op handle into every later test that mounts to document.body — document.body is one element object for the whole file and afterEach's innerHTML reset does not clear a WeakSet. This materialized once during the task's own development. Before B32 a missed unmount had no effect; now it poisons downstream tests. Fixing it means refactoring pre-existing tests, which the batch forbids, so it is correctly not fixed here. Flagging for the final review to triage — a shared afterEach that force-clears the WeakSet would need a test-only hook on the module.
- Task 21: minor (deferred): the report bolds the valid-prefix test "load-bearing" while its own following sentences correctly reason it is a pin. Honest disclosure, misleading headline; report-only.
- Task 21: minor (deferred): no dedicated test in the B36 block asserts "classPrefix omitted -> no warning". That requirement is covered only incidentally by an unrelated Task 20 test that shares a warn spy, so a future edit there could silently drop the only coverage. Real protection today; rated Minor by the reviewer and left for the final review to triage.
- Task 26: minor (deferred): a "(warns — see below)" comment refers forward to a paragraph two subsections later rather than anything in its own subsection. Lands on a real explanation; slightly indirect.
- Task 28: minor (deferred): the reworded classPrefix message is accurate but dense; could split into two sentences.
